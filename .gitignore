# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*
!.env.example

# Android / Capacitor
android/.gradle
android/build
android/app/build
android/local.properties
android/.idea
android/app/src/main/assets/public
android/app/src/main/assets/capacitor.config.json
*.apk

# Android signing key — kept local pending decision on storing it in the repo
# (see signing options discussion). Remove this line + `git add` the keystore
# to enable stable-signed CI builds.
android/app/tradelog-release.keystore
